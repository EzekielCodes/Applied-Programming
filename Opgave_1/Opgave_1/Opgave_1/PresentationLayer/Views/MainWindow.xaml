<Window x:Class="PresentationLayer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer"
        xmlns:viewmodels="clr-namespace:PresentationLayer.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"        
        
        mc:Ignorable="d"
        Title="{Binding Title}" d:Title ="Designtime Title"
        Height="450" Width="800">

    <DockPanel LastChildFill="false">
        <TextBlock DockPanel.Dock="Top" 
                   Text="{Binding Time}" d:Text="00:00:00" 
                   TextAlignment="Center" Background="CadetBlue" Foreground="White"
                   FontSize="24"  Margin="20" Width="300" Height="45" Padding="5" />
        <Button DockPanel.Dock="Top" Content="Update Time" Command="{Binding UpdateCommand}"  Width="100" Height="40"/>
        
    </DockPanel>
</Window>
